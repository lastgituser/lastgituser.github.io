export class HeroInfo {
    title: string;
    titleWidth: string;
    mockup: string;

    constructor(title: string, titleWidth: string, mockup: string) {
        this.title = title;
        this.titleWidth = titleWidth;
        this.mockup = mockup;
    }
}