export class NumberInfo {
    colorCode: string;
    number: number;
    desc: string;

    constructor(colorCode: string, number: number, desc: string) {
        this.colorCode = colorCode;
        this.number = number;
        this.desc = desc;
    }
}