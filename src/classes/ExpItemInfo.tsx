export class ExpItemInfo {
    img: string;
    num: number;
    desc: string;

    constructor(img: string, num: number, desc: string) {
        this.img = img;
        this.num = num;
        this.desc = desc;
    }
}